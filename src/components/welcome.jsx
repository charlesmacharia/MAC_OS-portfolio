import { useRef } from "react";

export const welcome_code = () => {

    const titleRef = useRef(null);
    const subtitleRef = useRef(null);
    return (
        <section
            id="welcome"
>     
            <div>
            <p >hi i'm macharia .Welcome to my</p>
            <h1>portfolio</h1>
</div>
        </section>
    )


}
